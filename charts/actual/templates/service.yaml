apiVersion: v1
kind: Service
metadata:
  name: {{ include "actual.fullname" . }}
  namespace: {{ include "actual.namespace" . }}
  labels:
    {{- include "actual.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "actual.selectorLabels" . | nindent 4 }}
