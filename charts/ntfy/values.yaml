# Default values for ntfy.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: binwiederhier/ntfy
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v2.17.0"

# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This is for setting up a config map more information can be found here: https://kubernetes.io/docs/concepts/configuration/configmap/
configMap:
  enable: true
  data:
    server.yml: |
      base-url: https://ntfy.example.com
      behind-proxy: true

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: LoadBalancer
  port: 80

# This block is for setting up the ingress for more information can be found here: https://kubernetes.io/docs/concepts/services-networking/ingress/
ingress:
  enabled: true
  className: ""
  annotations: {}
    # nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # nginx.ingress.kubernetes.io/whitelist-source-range: 192.168.13.0/24,10.8.0.0/24
    # cert-manager.io/cluster-issuer: lets-encrypt
  hosts:
    - host: ntfy.example.com
      paths:
        - path: /
          pathType: Prefix
  tls: []
  #  - secretName: ntfy-ingress-tls
  #    hosts:
  #      - ntfy.example.com

resources:
  limits:
    memory: "128Mi"
    cpu: "500m"
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}
