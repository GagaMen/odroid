apiVersion: v1
kind: Service
metadata:
  name: {{ include "adguard.fullname" . }}
  namespace: {{ include "adguard.namespace" . }}
  labels:
    {{- include "adguard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range $name, $config := .Values.service.ports }}
    {{- if $config.enabled }}
    - name: {{ $name }}
      port: {{ $config.port }}
      protocol: {{ $config.protocol }}
      targetPort: {{ $name }}
    {{- end }}
    {{- end }}
  selector:
    {{- include "adguard.selectorLabels" . | nindent 4 }}
---
{{- if .Values.prometheusExporter.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "adguard-prometheus-exporter.fullname" . }}
  namespace: {{ include "adguard.namespace" . }}
  labels:
    {{- include "adguard-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.prometheusExporter.service.type }}
  ports:
    - name: http
      port: {{ .Values.prometheusExporter.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "adguard-prometheus-exporter.selectorLabels" . | nindent 4 }}
{{- end -}}