[Journal]
# Storage=auto (the default) silently falls back to volatile storage in /run when
# it hits trouble, losing every log on reboot. persistent fails visibly instead.
Storage=persistent

# /var/log/journal had grown to 4.1 GB, sitting right at the 4 GiB default cap.
SystemMaxUse=1G
MaxRetentionSec=1month
